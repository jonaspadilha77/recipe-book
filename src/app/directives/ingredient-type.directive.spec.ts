import { IngredientTypeDirective } from './ingredient-type.directive';

describe('IngredientTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new IngredientTypeDirective();
    expect(directive).toBeTruthy();
  });
});
