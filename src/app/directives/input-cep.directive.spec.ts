import { InputCepDirective } from './input-cep.directive';

describe('InputCepDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCepDirective();
    expect(directive).toBeTruthy();
  });
});
